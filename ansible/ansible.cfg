[defaults]
inventory = inventory/
roles_path = roles/
host_key_checking = False
retry_files_enabled = False
vault_password_file = ../.vault-password
inventory_ignore_patterns = \.example$
hash_behaviour = merge
