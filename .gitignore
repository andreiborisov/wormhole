# Local inventory — contains hosts, IPs, URLs, and per-host vars; never commit
ansible/inventory/hosts.local.yml
ansible/inventory/host_vars/

# Generates files
ansible/local/

# Local VLESS test client (contains live Reality credentials)
local-vless-client/config.json

# Ansible Vault password file
.vault-password

# Make sure to include VS Code settings
!.vscode

# Specstory history
.specstory/
